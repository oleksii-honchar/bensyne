# Watch Source Configuration

Each watch source in watchSources defines a directory to monitor and its
chunking strategy. The agent-sessions source uses the agent-sessions
strategy. The obsidian source uses the obsidian strategy.

The strategy field determines which chunking strategy is applied to files
in that watch source.
