# Mnemosyne Environment Variables

The following environment variable names configure the embedding model and dimension:

- MNEMOSYNE_EMBEDDING_MODEL — the embedding model name (puma-embed-qwen3-4b)
- MNEMOSYNE_EMBEDDING_DIM — the embedding dimension (2560)
- MNEMOSYNE_EMBEDDING_API_URL — the LiteLLM gateway URL
